# GitLab Migration Tool Environment Variables

# Source GitLab Instance
SOURCE_GITLAB_URL=https://gitlab-source.example.com
SOURCE_GITLAB_TOKEN=your-source-personal-access-token

# Destination GitLab Instance
DEST_GITLAB_URL=https://gitlab-dest.example.com
DEST_GITLAB_TOKEN=your-destination-personal-access-token

# Migration Settings
MIGRATION_BATCH_SIZE=50
MIGRATION_MAX_WORKERS=5
MIGRATION_TIMEOUT=300

# Logging
LOG_LEVEL=INFO
LOG_FILE=migration.log

# Optional: Database for state persistence (for large migrations)
# DATABASE_URL=sqlite:///migration_state.db

# Optional: Encryption key for credential storage
# ENCRYPTION_KEY=your-32-byte-base64-encoded-key
